- if list.any?
  %table.admin 
    %th Название
    %th Описание
    %th
    - list.each do |c|
      %tr
        %td.b.l= link_to c.name, c
        %td.i.l= c.description
        %td= link_to 'Удалить', c,
                      :confirm => 'Вы действительно этого хотите?',
                      :method  => :delete,
                      :class   => :red
- else
  .red.i.b Нет дочерних категорий для этой категории
